<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Formulario POST</title>

    <!-- Bootstrap-->
    <link href="/vendor/bower/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body style="background-color: <%= color %>">

<div class="container">
    <div class="row">
        <div class="col-sm-6 col-md-4">
            <h1>Formulario Session</h1>
            <form class="form-horizontal" action="/procesaForm" method="post" name="formularioPost">
                <div class="form-group">
                    <label for="nombre">Escribe "Supercalifragilisticoespiralidoso"</label>
                    <input class="form-control" type="text" name="nombre" placeholder="Nombre" id="nombre">
                </div>
                <% if(mostrar==1){ %>
                <div class="alert alert-dismissible alert-<%= tipo %>">
                    <button type="button" class="close"
                            data-dismiss="alert" aria-hidden="true">&times;</button>
                    <strong><%= intro %></strong> <%= mensaje %>
                </div>
                <% } %>
                <div class="form-group">
                    <label for="color">Selecciona un color de fondo</label>
                    <input class="form-control" type="color" name="color"
                           placeholder="Elige un color" id="color" value="<%= color %>">
                </div>
                <button type="submit" class="btn btn-default">Enviar</button>
            </form>
        </div>
        <div class="col-sm-1 col-md-1"></div>
        <div class="col-sm-5 col-md-7">
            <h1>Numero de visitas: <%= visitas %></h1>
        </div>
    </div>
</div>

<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="/vendor/bower/jquery/dist/jquery.min.js"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="/vendor/bower/bootstrap/dist/js/bootstrap.min.js"></script>
</body>
</html>